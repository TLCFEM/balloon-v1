\section{Towards A Better One}
\subsection{History of Load Reversals}
In conventional plasticity models, local reversals cannot be easily detected.
A common criterion for detecting reversals is the double contraction $\beeta:\dot{\bs}$.
For example, in the multi-surface theory \citep{Semenov2022}, the exact criterion is used to identify the initiation of a load reversal, which further controls the activation of a new surface.

The normal yield ratio $z$ effectively represents the current `loading level' at any given moment.
By definition, this loading level is normalised between \num{0} and \num{1} thus does not depend on the current size of the yield surface: when it increases, the subloading surface expands, indicating a loading process; when it decreases, the subloading surface contracts, indicating an unloading process.
Thus, load reversals can be detected with ease by checking whether $z$ ever decreases within the given (sub-)step.

For a material (point) that has experienced up to $N$ load reversals, let the normal yield ratio at each load reversal be denoted as $z_r^j$ with $j=1,2,3,\cdots,N$ standing for the $j$-th reversal.
The initial value is $z_r^0=0$.
We use $z_r$ to denote some measure of the entirety of $z_r^j$, thus,
\begin{gather}
    \boxed{z_r=Z\left(\left\{z_r^j\right\}_{j=0}^N\right)=Z\left(z_r^0,z_r^1,z_r^2,\cdots,z_r^N\right).}
\end{gather}
In the above, $Z\left(\cdot\right)$ is a function that maps the collection $\left\{z_r^j\right\}_{j=0}^N$ to a single scalar $z_r$.
Depending on the specific choice of function $Z$, $z_r$ embodies different memory effects.
For example, $z_r$ can take the maximum value of the most recent $M$ values.
When $M=1$, $z_r$ is the most recent value $z_r^N$.
When $M\geqslant{}N$, $z_r$ is simply the maximum value of all $z_r^j$.
\subsection{Additive Decomposition of Plastic Strain}
It is possible to split the plastic strain $\bvarepsilon^p$ into two parts,
\begin{gather}\label{eq:split_q}
    \boxed{\bvarepsilon^p=\bvarepsilon^{p,m}+\bvarepsilon^{p,c},}
\end{gather}
with $\bvarepsilon^{p,m}$ denoting the `monotonic' part and $\bvarepsilon^{p,c}$ denoting the `cyclic' part.
The rationale behind this split is straightforward: the accumulated plastic strain $q$ defined in \eqsref{eq:q_original} only tracks the total amount of plastic deformation, although it is non-decreasing, there is no way to infer the corresponding loading process.
The split \eqsref{eq:split_q} allows one to identify the plastic strain accumulated during monotonic loading and that accumulated during load reversals.
This makes it possible to account for various effects such as rearrangement of dislocations.
It is reasonable to assume that the plastic strain increment $\dot{\bvarepsilon}^p$ within a given (sub-)step would be distributed between two parts according to some rule, which may depend on the current loading level $z$, and/or its history $z_r$.
Without loss of generality, one can thus define
\begin{gather}\label{eq:split_q_definition}
    \dot{\bvarepsilon}^{p,m}=K\dot{\bvarepsilon}^p,\qquad
    \dot{\bvarepsilon}^{p,r}=\dot{\bvarepsilon}^p-\dot{\bvarepsilon}^{p,m}=\left(1-K\right)\dot{\bvarepsilon}^p,
\end{gather}
where $K=K\left(z,z_r\right)$ is a function of $z$ and $z_r$ (or equivalently, $\left\{z_r^j\right\}_{j=0}^N$) that yields a split ratio between \num{0} and \num{1}.
Accordingly, a similar split can be applied to $q$,
\begin{gather}
q=q_m+q_r,\qquad
\dot{q}_m=K\dot{q},\qquad
\dot{q}_r=\left(1-K\right)\dot{q},
\end{gather}

Then the isotropic bound $F$ can be accordingly defined as a function of $\dot{q}_m$ instead,
\begin{gather}\label{eq:f_revised}
\boxed{F=F\left(q_m\right).}
\end{gather}
\eqsref{eq:f_revised} effectively loosens the coupling between $F$ and $q$.
By varying $K$, it is possible to control the portion of $\dot{\bvarepsilon}^p$ that contributes to the evolution of $F$.


\begin{table}[htb]
    \centering\footnotesize
    \caption{Summary of the proposed formulation}\label{tab:summary}
    \begin{tabular}{rl}
        \toprule
        Constitutive Law         & $\bsigma=\mb{D}_e:\left(\bvarepsilon-\bvarepsilon^p\right)$                                                                                           \\\midrule
        Subloading Surface       & $f_s=\norm{\beeta}-z\sqrt{\dfrac{2}{3}}\sigma^y$                                                                                                      \\[2mm]
                                 & $\beeta=\dev{\bsigma}-a^y\balpha+\left(z-1\right)\sigma^y\bd$                                                                                         \\\midrule
                                 Flow Rule                & $\dot{\bvarepsilon^p}=\gamma{}\bn$                                                                                                                    \\\midrule
                                 Evolution Rules          & $\dot{q}=\sqrt{\dfrac{2}{3}}\gamma$                                                                                                                   \\[2mm]
                                 & $\dot{z}=U\dot{q}$                                                                                                                                    \\[2mm]
                                 & $\dot{\balpha}=b\left(\sqrt{\dfrac{2}{3}}\bn-\balpha\right)\gamma$                                                                                    \\[2mm]
                                 & $\dot{\bd}=c_e\left(\sqrt{\dfrac{2}{3}}z_e\bn-\bd\right)\gamma$                                                                                       \\[2mm]
                                 & $a^y=a^i+k_\text{kin}q+a^s_\text{kin}\left(1-e^{-m^s_\text{kin}q}\right)$                                                                             \\\midrule
        New Proposal\faLightbulb & $\sigma^y=\sigma^y_m+\sigma^y_r$                                                                                                                      \\[2mm]
        New Proposal\faLightbulb & $\sigma^y_m=\sigma^i_m+k_{m,\text{iso}}q_m+\sigma^s_{m,\text{iso}}\left(1-e^{-m^s_{m,\text{iso}}q_m}\right)$ with $\dot{q_m}=Q\dot{q}$                \\[2mm]
        New Proposal\faLightbulb & $\sigma^y_r=\sigma^i_r+k_{r,\text{iso}}q_r+\sigma^s_{r,\text{iso}}\left(1-e^{-m^s_{r,\text{iso}}q_r}\right)$ with $\dot{q_r}=\left(1-Q\right)\dot{q}$ \\\bottomrule
    \end{tabular}
\end{table}

Compared to the previous formulation \citep{Chang2025}, we have revised the isotropic hardening rule by introducing a multiplicative split of the accumulated plastic strain.
As a result, the isotropic bound $\sigma^y$ is function of not only $q$ but also $z$ and its history.

\subsection{Stagnation}
\citet[][see, \S~2.4]{Yoshida2002} introduces an additional \textit{non-isotropic-hardening} surface in the stress space such that isotropic hardening can be activated/deactivated based on the size of the back stress, thus the kinematic hardening.
One shall be aware of the fact that this approach would not work if kinematic hardening is not defined.
Since the evolution of the back stress is eventually driven by the plastic strain, such an approach can be equivalently formulated in the strain space.
\citet{Ohno1982,Chaboche2008,Hashiguchi2023a} all adopt a strain space formulation.
\subsection{Objective Transition}
However, $z$ is only a measure of the relative loading level, inevitably, the loading curve tends to be steeper when the yield stress is large, and vice versa.
To make this transition more objective, less dependent on material parameters, we propose the following enhancement.
\begin{gather}\label{eq:z_evolution_revised}
    \dot{z}=\dfrac{U}{H}\dot{q}.
\end{gather}